# Generated static site (rendered by the action; published to Pages).
/site/

# Node.
/node_modules/
*.tsbuildinfo

# Note: dist/ is intentionally committed. GitHub Actions resolves a JS
# action's `main:` from the tag's tree, so the bundled artifact must ship
# alongside the source. Re-run `npm run build` and commit dist/ before
# tagging a release.
